# Database Credentials
# You will replace the values on the right of the equals sign with YOUR actual credentials.

# OPTION 1: Using a single connection URL (Common and cleaner for deployment)
# Replace the entire string inside the quotes with your local PostgreSQL connection string.
# DATABASE_URL="postgresql://[your_user]:[your_password]@[your_host]:[your_port]/[your_db_name]"

# --- OR ---

# OPTION 2: Using separate credentials (If your database library requires them separately)
# Replace the placeholder values with your local DB settings.
DB_USER="postgres"
DB_PASSWORD="postgres123"
DB_HOST="localhost"
DB_NAME="todolist"
DB_PORT=5432